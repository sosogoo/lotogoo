package main

func main() {
	// const (
	// 	filename = "big.txt"
	// 	n        = 1000
	// )
	// file, err := os.Create(filename)
	// if err != nil {
	// 	panic(err)
	// }
	// p := RandomSend(n)              //生成随机数p  发送p至chan
	// writer := bufio.NewWriter(file) //缓冲写对象
	// WriterSink(writer, p)
	// writer.Flush() //将p写入对象
	// file, err = os.Open(filename)
	// if err != nil {
	// 	panic(err)
	// }
	// // reader := bufio.NewReader(file) //缓冲读对象
	// p = ReadSource(bufio.NewReader(file))

	// for v := range p {
	// 	fmt.Println(v)
	// }

}
